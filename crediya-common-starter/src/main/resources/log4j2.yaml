Configuration:
  status: WARN
  properties:
    Property:
      - name: LOG_PATH
        value: ${sys:LOG_PATH:-./logs}
      - name: SERVICE_NAME
        value: ${sys:SERVICE_NAME:-microservice}
      - name: APP_PACKAGE
        value: ${sys:APP_PACKAGE:-com.example}

  Appenders:
    Console:
      name: Console
      target: SYSTEM_OUT
      JsonTemplateLayout:
        eventTemplateUri: "classpath:log4j2-template.json"

    RollingFile:
      name: RollingFile
      fileName: "${LOG_PATH}/${SERVICE_NAME}.log"
      filePattern: "${LOG_PATH}/${SERVICE_NAME}-%i.log.gz"
      JsonTemplateLayout:
        eventTemplateUri: "classpath:log4j2-template.json"
      Policies:
        SizeBasedTriggeringPolicy:
          size: "10MB"
      DefaultRolloverStrategy:
        max: "5"

  Loggers:
    Logger:
      # Logger dinámico para el package de la aplicación
      - name: ${APP_PACKAGE}
        level: ${sys:APP_LOG_LEVEL:-debug}
        includeLocation: true
        additivity: false
        AppenderRef:
          - ref: Console
          - ref: RollingFile

      - name: org.springframework
        level: ${sys:SPRING_LOG_LEVEL:-info}
        additivity: false
        AppenderRef:
          - ref: Console
          - ref: RollingFile

      - name: org.springframework.security
        level: ${sys:SECURITY_LOG_LEVEL:-info}
        additivity: false
        AppenderRef:
          - ref: Console
          - ref: RollingFile

      - name: io.r2dbc
        level: ${sys:R2DBC_LOG_LEVEL:-warn}
        additivity: false
        AppenderRef:
          - ref: Console
          - ref: RollingFile

      - name: reactor.netty
        level: ${sys:NETTY_LOG_LEVEL:-warn}
        additivity: false
        AppenderRef:
          - ref: Console
          - ref: RollingFile

    Root:
      level: ${sys:ROOT_LOG_LEVEL:-info}
      AppenderRef:
        - ref: Console
        - ref: RollingFile